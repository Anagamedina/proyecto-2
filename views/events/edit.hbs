<div>
    <form action="/events/{{events._id}}/edit" method="POST">
        <input name="title" type="text" placeholder="title" value="{{events.title}}" > <br>
        <textarea name="description" cols="30" rows="3" placeholder="description">{{events.description}}</textarea> <br>
        <input name="location" type="text" placeholder="location" value="{{events.location}}" > <br>
        <input name="date" type="date" placeholder="date" value="{{events.date}}" > <br> 
{{!-- http://localhost:3000/events/new --}}
    <button>Submit</button>

    </form>
</div>